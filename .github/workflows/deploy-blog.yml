name: deploy-blog

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: 🛎 Check out master
        uses: actions/checkout@master
        with:
          fetch-depth: 1
      - name: 🧪 Build and deploy
        uses: victoriadrake/jekyll-cd@bb775fd060be80d8f295d9214c7f98f871413809
      - name: copy files via ssh key
        uses: appleboy/scp-action@edc8ec9139a2687bcebf0249d0352ff2a988df00        
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          source: "docs/*"
          target: "/home/opc/temp/"
      - name: executing remote ssh commands using key
        uses: appleboy/ssh-action@fd39ef0f1814d986b5d9c5246ad0414060fe2e14
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          script: |
            sudo rm -rf /var/www/html/*
            sudo mv /home/opc/temp/docs/* /var/www/html/
